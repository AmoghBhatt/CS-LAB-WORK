#include<string.h>
#include<stdio.h>
main()
{
    char s1[30],s2[30];
    int res,i,l1,l2,flag;
    printf("input elements of 1st");
    gets(s1);
    printf("input elements of 2nd");
    gets(s2);
    l1=strlen(s1);
    l2=strlen(s2);
    if(l1!=l2)
    printf("strings unmatched");
    else
    {
        i=0;
        while(s1[i]='\0')
        {
            if(s1[i]!=s2[i])
            {
                flag=1;
                break;
            }
        }
        
    if(flag==1)
    printf("not equal");
    else
    printf("equal");
    }
}
